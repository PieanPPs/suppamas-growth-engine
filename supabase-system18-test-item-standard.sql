-- =============================================================
-- System 18: เก็บ "มาตรฐาน" แยกจาก "รหัสตัวชี้วัด" ในแต่ละข้อสอบ
-- รหัสตัวชี้วัดเดียวกัน (เช่น "ม.1/1") ใช้ซ้ำได้ในหลายมาตรฐานที่มีเนื้อหาต่างกันโดยสิ้นเชิง
-- ถ้าไม่เก็บมาตรฐานแยกไว้ต่อข้อ จะแยกไม่ออกว่าข้อไหนตั้งใจวัดตัวชี้วัดตัวไหนจริง ๆ
-- (สรุป "แยกตามตัวชี้วัด" ในหน้าพิมพ์ข้อสอบ และการ sync test_indicators ต้องใช้ฟิลด์นี้)
--
-- รันซ้ำได้ปลอดภัย — รันใน Supabase SQL Editor
-- =============================================================

ALTER TABLE test_items ADD COLUMN IF NOT EXISTS standard TEXT;
